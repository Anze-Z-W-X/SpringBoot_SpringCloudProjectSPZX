spring:
  application:
    name: service-user # 服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.164.129 # nacos地址
      config:
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - dataId: service-user-dev.yaml